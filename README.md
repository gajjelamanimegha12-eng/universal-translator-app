# universal-translator-app
Universal Translator is a Streamlit-based multilingual application that automatically detects the input language, translates text into multiple languages, generates word-cloud visualizations, and provides text-to-speech output for translated content.
